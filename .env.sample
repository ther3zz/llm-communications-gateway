# --- Core Configuration ---
# Public URL of your server (Required for Telnyx Webhooks)
# e.g. https://my-server.ngrok.io
BASE_URL=

# Debug Mode (true/false) - Enables verbose logs and startup config printing
DEBUG=false

# --- LLM Configuration ---
# Default Provider: 'openai', 'ollama', 'openwebui', or 'custom'
DEFAULT_LLM_PROVIDER=openwebui

# Provider Specific URLs
# OLLAMA_URL=http://host.docker.internal:11434
# OPEN_WEBUI_URL=http://host.docker.internal:3000

# Legacy/Generic URL (Fallback)
# LLM_URL=http://host.docker.internal:11434/v1

# Model Name
LLM_MODEL=llama3:8b

# API Key (if required by provider)
# LLM_API_KEY=sk-...

# System Prompt
# SYSTEM_PROMPT="You are a helpful assistant..."

# --- Voice Services ---
# Speech-to-Text (Parakeet)
STT_URL=http://parakeet:8000
STT_TIMEOUT=10

# Text-to-Speech (Chatterbox)
TTS_URL=http://chatterbox:8000
TTS_VOICE_ID=default
TTS_TIMEOUT=10
LLM_TIMEOUT=10

# Audio Configuration
# RTP_CODEC: 'L16' (High Quality 16-bit 8kHz), 'PCMU' (Standard u-law), 'PCMA' (a-law)
RTP_CODEC=L16

# --- Telnyx Provider Seeding ---
# If set, these will auto-create/update the Telnyx provider in the DB on startup
# TELNYX_API_KEY=KEY0123...
# TELNYX_FROM_NUMBER=+15555555555
# TELNYX_APP_ID=12345678-90ab-cdef...

# --- Database ---
# Defaults to SQLite inside the container
# DATABASE_URL=sqlite:///backend/data/database.db

# --- Security ---
# Webhook Secret (auto-generated if empty, but can be fixed here)
# WEBHOOK_SECRET=my-secret-token

# Encryption for Database Keys
ENCRYPTION_ENABLED=true
SALT=change_this_to_a_random_string_for_production
